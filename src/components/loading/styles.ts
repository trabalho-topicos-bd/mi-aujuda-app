import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    lottieContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    loadingLottie: {
        width: 300,
        height: 400,
    },
    lottieText: {
        fontFamily: 'Medium',
        fontSize: 18,
        color: '#777',
    },
});
