import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    root: {
        marginTop: 16,
        borderRadius: 16,
        overflow: 'hidden',
        elevation: 4,
    },
    gradient: {
        padding: 16,
    },
    text: {
        fontFamily: 'Regular',
        fontSize: 20,
        color: '#FFFFFF',
        textAlign: 'center',
    },
});
